# Render fixture: empty bodies (steps throw NotImplementedException, no
# idempotency/business-incident wiring) — the migration-agent starting point.
#   intropy int create transactional -o /tmp/tx-empty -f transactional/examples/empty.yaml --version main --no-input
name: OrderSync
organization: Acme
empty: true
