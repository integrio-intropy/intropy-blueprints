apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: {{ .namespace }}
resources:
  - ../../base
  - pubsub.yaml
images:
  - name: {{ .image }}
    # Placeholder — bump from CI or by hand:
    #   kustomize edit set image {{ .image }}:<version>
    newTag: "0.1.0"
patches:
  - path: deployment-patch.yaml
